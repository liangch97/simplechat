# 模块 1: Java 网络编程基础

## 🎯 学习目标

- 理解 Socket 通讯的基本概念
- 掌握 Java 中 ServerSocket 和 Socket 的使用
- 学会创建简单的客户端-服务器程序

## 📖 核心概念

### 什么是 Socket？

Socket（套接字）是网络通信的端点。就像电话一样：
- **服务器** = 电话总机，等待来电
- **客户端** = 打电话的人，主动发起连接

### Java 网络编程核心类

```
┌─────────────────────────────────────────────────────────┐
│                    java.net 包                           │
├─────────────────────────────────────────────────────────┤
│  ServerSocket  - 服务器端，监听端口，等待连接             │
│  Socket        - 客户端/服务端通讯通道                    │
│  InetAddress   - IP 地址封装                             │
│  URL           - 统一资源定位符                          │
└─────────────────────────────────────────────────────────┘
```

### 通讯流程

```
  服务器 (Server)                    客户端 (Client)
       │                                  │
  ┌────▼────┐                             │
  │ 创建    │                             │
  │ServerSocket                           │
  │ 端口5050│                             │
  └────┬────┘                             │
       │                                  │
  ┌────▼────┐                        ┌────▼────┐
  │ accept()│ ◄─────── 建立连接 ─────│ 创建    │
  │ 阻塞等待│                        │ Socket  │
  └────┬────┘                        └────┬────┘
       │                                  │
       │◄──────────── 数据传输 ──────────►│
       │      (InputStream/OutputStream)  │
       │                                  │
  ┌────▼────┐                        ┌────▼────┐
  │ close() │                        │ close() │
  └─────────┘                        └─────────┘
```

## 📁 本模块文件

```
01-network-basics/
├── README.md           # 本文档
├── examples/
│   ├── SimpleServer.java    # 最简单的服务器
│   ├── SimpleClient.java    # 最简单的客户端
│   └── EchoServer.java      # 回声服务器
└── exercises/
    ├── Exercise1.md         # 练习题目
    └── solution/            # 参考答案
```

## ▶️ 下一步

查看 [examples/SimpleServer.java](./examples/SimpleServer.java) 开始学习！
